using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TemperatureCooling : MonoBehaviour
{
    // Start is called before the first frame update
    void Start()
    {
        // already in ethereal layer, no need to set
    }

    // Update is called once per frame
    void Update()
    {
        // change color each frame
        // when fully cooled, switch to ephemeral layer
    }
}
